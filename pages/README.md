# Pages

Les pages publiques utilisent les données initiales de `/js/seed-data.js`, le stockage local de `/js/data-store.js` et le rendu partagé de `/js/app.js`.
Les URL principales sont exposées par les dossiers racine (`/etablissements`, `/programmes`, `/agenda`, `/activites`, `/enseignants`).
